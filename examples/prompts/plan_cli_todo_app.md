# Prompt: plan_cli_todo_app

You are executing one Planfoldr model task.

Goal: create a tiny CLI todo-list project with tests.

Return only JSON matching the task output schema.

The project should include:
- a CLI entry point;
- add/list/done commands;
- tests that can run locally;
- no network dependency at test time.

Do not control the workflow. The runtime will decide the next task from your structured output.
